# Example htpasswd entry. Do NOT use in production. Generate with:
# docker run --rm httpd:2.4 htpasswd -Bbn admin 'your-password' > nginx/htpasswd
# Example content (replace with generated file):
# admin:$apr1$randomsalt$abcdefghijklmnopqrstuv

# Put the generated file at nginx/htpasswd (ensure it's not checked into git with secrets)
